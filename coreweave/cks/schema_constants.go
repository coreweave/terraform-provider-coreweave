package cks

const (
	schemaKeyStart     = "start"
	schemaKeyEnd       = "end"
	schemaKeyIssuerURL = "issuer_url"
	schemaKeyClientID  = "client_id"
	schemaKeyServer    = "server"
	logKeyError        = "error"
)
